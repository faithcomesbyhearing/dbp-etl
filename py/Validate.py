# Validate.py



class Validate:

	def __init__(self, config, args):
		## parse command line


	def process(self):
		## filenameParser
		## UpdateDBPDatabase
		##


	def prepareDirectory(self, directory):
		## ensure that it exists
		os.remove(directory + os.sep + "*.csv")








## parse command line
## config profile is required

config = Config(profile)
validate = Validate(config, args)
validate.process()